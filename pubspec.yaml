name: flutter_local_ci
description: A local CI pipeline for Flutter — run checks before pushing to your repo.
version: 0.1.0
homepage: https://github.com/SyedTahaAlam/flutter_preflight

environment:
  sdk: ">=3.0.0 <4.0.0"

executables:
  flutter_local_ci: flutter_local_ci

dependencies:
  args: ^2.4.0
  yaml: ^3.1.0
  path: ^1.8.0

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.0
  lints: ^3.0.0
